services:
    redis-ui:
        build:
            context: .
        ports:
            - '2222:3000'
        environment:
            - PORT=3000
        restart: unless-stopped
        volumes:
            - redis-ui-data:/app/data

volumes:
    redis-ui-data:
        driver: local
